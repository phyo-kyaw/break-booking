apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-booking-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service-booking-api
  template:
    metadata:
      labels:
        app: service-booking-api
    spec:
      containers:
        - name: springboot
          image: pkdocker3383/service-booking-api:v1
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
---
apiVersion: v1
kind: Service
metadata:
  name: service-booking-api
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: service-booking-api